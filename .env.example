# Google Search Console API Configuration
# To use Search Console features, you need to:
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable the Google Search Console API
# 4. Create OAuth 2.0 credentials (Web application type)
# 5. Add your redirect URL: http://localhost:3000/oauth2callback (for development)
# 6. Copy the Client ID and Client Secret below

SEARCH_CONSOLE_CLIENT_ID=your_google_client_id_here
SEARCH_CONSOLE_CLIENT_SECRET=your_google_client_secret_here
SEARCH_CONSOLE_REDIRECT_URL=http://localhost:3000/oauth2callback

# For production, use your actual domain
# SEARCH_CONSOLE_REDIRECT_URL=https://yourdomain.com/oauth2callback
